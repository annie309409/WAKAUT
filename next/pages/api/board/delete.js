import Board from  "../../../models/board"

export default async(req,res)=>{
    // let cnt = '';
    console.log('서버입력',req.body);
    // cnt =  new Board().modify(req.body).then(res=>res);
    // res.status(200).json({cnt: await cnt});
}
